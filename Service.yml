apiVersion: v1
kind: Service
metadata:
  name: yogast-deployment
  labels:
    app: yogast
spec:
  ports:
    - prot: 8080
      targetport: 80
      nodeport: 31200
  selector:
    app: yogast
  type: LoadBalancer  
